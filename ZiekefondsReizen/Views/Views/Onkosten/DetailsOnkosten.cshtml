@model OnkostenDetailsViewModel
@{
    ViewData["Title"] = "Onkosten Details";
}

<h2>Details voor Onkost: @Model.Titel</h2>
<br />
<table class="table">
    <tr>
        <td>ID</td>
        <td>@Model.Id</td>
    </tr>
    <tr>
        <td>Titel</td>
        <td>@Model.Titel</td>
    </tr>
    <tr>
        <td>Omschrijving</td>
        <td>@Model.Omschrijving</td>
    </tr>
    <tr>
        <td>Bedrag</td>
        <td>€ @Model.Bedrag</td>
    </tr>
    <tr>
        <td>Datum</td>
        <td>@Model.Datum.ToString("dd-MM-yyyy")</td>
    </tr>
</table>

<br />

<a asp-action="Index" class="btn btn-primary">Terug naar de lijst</a>
